import React from 'react';
import {View, Text} from 'react-native';
import Header from './components/Header'

const Home = () => {
  return (
    <View style={{flex:1}}>
      <Header />
    </View>
  );
};

export default Home;
